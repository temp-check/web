<h2>Results</h2>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @searches.each do |search| %>
  <tr>
    <td><%= search.updated_at.in_time_zone("Pacific Time (US & Canada)").strftime("%m/%d/%y @ %I:%M %p").downcase %></td>
    <td><%= search.cached ? '💵' : '🔥' %></td>
    <td class="align-left">
      <%= search.address %>
    </td>
    <td><%= search.error.nil? ? '✅' : '❌' %></td>
    <td><%= link_to 'View', search %></td>
  </tr>
  <% end %>
</table>

<%= link_to "New", new_search_path %>
